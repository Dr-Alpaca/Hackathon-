"""配置文件 - 食堂列表、关键词、LLM配置"""

# 食堂全称列表 - 根据实际情况修改
CANTEENS = [
    "一食堂",
    "二食堂",
    "三食堂",
    "四食堂",
    "五食堂",
]

# 食堂别名映射 -> 全称
CANTEEN_ALIAS = {
    "一": "一食堂",
    "二": "二食堂",
    "三": "三食堂",
    "四": "四食堂",
    "五": "五食堂",
}

# 菜品后缀关键词，用于匹配菜品名
DISH_SUFFIXES = [
    "饭", "面", "粉", "汤", "饼", "包", "粥",
    "套餐", "米线", "凉皮", "饺子", "包子", "馒头",
]

# 情绪/标签关键词映射
EMOTION_KEYWORDS = {
    "出餐快": ["快", "快了", "很快", "出餐快", "不排队"],
    "出餐慢": ["慢", "很慢", "出餐慢", "排队久", "等很久"],
    "量大": ["量大", "量大", "很多", "管饱", "分量足", "够吃"],
    "量少": ["量少", "不够吃", "分量小"],
    "便宜": ["便宜", "不贵", "划算", "块钱", "元"],
    "贵": ["贵", "不便宜", "价格高"],
    "辣": ["辣", "很辣", "麻辣", "香辣"],
    "清淡": ["清淡", "不辣", "原味"],
    "排队久": ["排队", "人多", "挤", "等很久"],
}

# LLM 配置
LLM_CONFIG = {
    "api_key_env": "OPENAI_API_KEY",
    "base_url": "https://open.bigmodel.cn/api/paas/v4/",
    "model": "glm-4-flash",
    "timeout": 10,
}
# 后期更换模型只需要修改这里的 model 和 base_url 即可，或者通过环境变量覆盖

# LLM 系统Prompt
SYSTEM_PROMPT = """你是一个餐饮信息提取助手。你的任务是从用户描述的校园食堂用餐体验中，提取出关键信息，并以严格的JSON格式返回。

输入用户帖子文本（可能包含口语、标点、图片描述等）。

输出格式
你必须只返回一个JSON对象，不要包含任何其他解释、标记或文本。

JSON结构如下：
{
  "canteen": "食堂名称（字符串，如果没有则填null）",
  "shop_name": "窗口/店铺名（字符串，如果没有则填null）",
  "dish_name": "菜品名（字符串，如果没有则填null）",
  "quote": "一句简短的精彩评价摘录（约10-30字，取最有画面感的部分）",
  "tags": ["标签1", "标签2"]
  // 标签从['出餐快','出餐慢','量大','量少','便宜','贵','辣','清淡','排队久']中选择，最多3个，如果没有则空数组
}

示例输入：
"一食堂三楼的7号窗口，铁板黑椒鸡排饭真的绝了！鸡排是现煎的，黑椒汁给得足，12块钱还能加个蛋。就是排队人太多了，每次都要等10分钟。"

输出：
{
  "canteen": "一食堂三楼",
  "shop_name": "7号窗口",
  "dish_name": "铁板黑椒鸡排饭",
  "quote": "鸡排是现煎的，黑椒汁给得足",
  "tags": ["量大", "便宜", "排队久"]
}

现在开始，请仅返回JSON。"""
